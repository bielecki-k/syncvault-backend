services:
  postgres:
    image: postgres:18
    container_name: sv_postgres
    environment:
      POSTGRES_DB: sv_database
      POSTGRES_PASSWORD: password
      POSTGRES_USER: user
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: