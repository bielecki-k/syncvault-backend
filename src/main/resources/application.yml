management:
  endpoints:
    web:
      expousure:
        include: health
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/sv_database
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  flyway:
    enabled: true

server:
  port: 8080